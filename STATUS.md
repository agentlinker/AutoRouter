# 项目状态

## 一、架构健康度

- 模块总数：14，以源码目录的一级子目录计。
- 违规跨模块调用：尚未完成全仓审计，不能报告为零。
- 协议硬过滤已接入三个网关入口；协议专用适配器和账户身份投影尚未完成。

## 二、本次变更影响范围

- 配置：仅接受三个精确协议；运行时投影校验持久化协议。
- 数据库：旧协议和端点标识事务迁移，清除不明来源的旧观测；新增账户与端点的权限状态表。
- 仓储：支持组合开关、错误、成功和清除状态；启用候选查询排除被禁用或不可用的组合。
- 路由：移除协议加分，硬过滤异协议候选；删除响应接口的跨协议转换分支。
- 文件：涉及配置、数据库、仓储、运行时、路由、三个网关入口、适配器注册表及相关测试；任务清单位于原变更目录。
- 接口契约：已改变配置枚举和路由选择参数；管理接口尚待同步，不可发布。

## 三、已知风险点

- 定向验证：6 个文件、38 项测试通过；类型检查及管理前端构建通过。
- 全量验证：276 项测试中 198 项通过、78 项失败，涉及 14 个文件；旧配置和管理接口仍使用旧协议。
- 管理界面、官方模板、结构化错误归因、消息接口转换清理、账户身份投影和状态控制接线尚未完成。
- 未完成逐断言变异验证、全仓遗留符号清理和最终验收；不能将任务清单的未勾选项视为完成。
- 重复协议身份会使迁移失败并回滚，不会自动删除或猜测应保留的端点。
- 前端构建仍报告大于 500 千字节的资源块，未做范围外拆包优化。

## 四、下次最该做的事

统一仓储、管理接口和模板的精确协议契约，随后完成协议专用适配器、剩余转换删除与结构化状态归因，逐步恢复全量测试。
